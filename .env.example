# ═══════════════════════════════════════════════════════════════════════════════
# SOC Automation Bot - Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in the values.
# Values marked as "MOCK" will use simulated responses (safe for development).

# ─── Application ──────────────────────────────────────────────────────────────
APP_ENV=development
APP_DEBUG=true
API_KEY=dev-secret-key-change-in-production
SECRET_KEY=change-me-to-a-random-32-char-string

# ─── Database ─────────────────────────────────────────────────────────────────
# Create a free project at https://supabase.com
# Use the "Connection String" (URI) they provide in your project settings.
# It should look something like: postgresql://postgres.abcdefg:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
DATABASE_URL=MOCK_REPLACE_WITH_SUPABASE_URL

# ─── Redis ────────────────────────────────────────────────────────────────────
# The bot requires Redis for background task queuing.
# Create a free project at https://upstash.com
# Use the "Rediss" (TLS) URL they provide.
# It should look something like: rediss://default:[PASSWORD]@cool-redis-1234.upstash.io:6379
REDIS_URL=MOCK_REPLACE_WITH_UPSTASH_URL
CELERY_BROKER_URL=MOCK_REPLACE_WITH_UPSTASH_URL
CELERY_RESULT_BACKEND=MOCK_REPLACE_WITH_UPSTASH_URL

# ─── Threat Intelligence APIs ─────────────────────────────────────────────────
# Replace "MOCK" with your actual API key when available.
# The system will use realistic simulated responses when set to MOCK.

# VirusTotal - https://www.virustotal.com/gui/my-apikey
VIRUSTOTAL_API_KEY=MOCK

# AbuseIPDB - https://www.abuseipdb.com/account/api
ABUSEIPDB_API_KEY=MOCK

# AlienVault OTX - https://otx.alienvault.com/api
OTX_API_KEY=MOCK

# ─── Enrichment Cache ─────────────────────────────────────────────────────────
ENRICHMENT_CACHE_TTL=86400  # 24 hours in seconds

# ─── Notification APIs ────────────────────────────────────────────────────────
# Slack Incoming Webhook - https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=MOCK
SLACK_ALERT_CHANNEL=#security-alerts

# ─── Ticketing ────────────────────────────────────────────────────────────────
# Jira - https://support.atlassian.com/atlassian-account/docs/manage-api-tokens-for-your-atlassian-account/
JIRA_URL=MOCK
JIRA_EMAIL=MOCK
JIRA_API_TOKEN=MOCK
JIRA_PROJECT_KEY=SOC

# ─── Flower (Celery Monitoring) ───────────────────────────────────────────────
FLOWER_USER=admin
FLOWER_PASSWORD=flower123

# ─── Risk Scoring Thresholds ──────────────────────────────────────────────────
RISK_SCORE_LOW_THRESHOLD=20
RISK_SCORE_MEDIUM_THRESHOLD=40
RISK_SCORE_HIGH_THRESHOLD=70
RISK_SCORE_CRITICAL_THRESHOLD=90
